package de.eldoria.forestsaver.configuration.elements;

public enum ResourceType {
    /**
     * Represents a resource, which is growing fragment.
     */
    GROWING,
    /**
     * Represents a resource, which is a fragment.
     */
    FRAGMENT,
    /**
     * Represents a resource, which is a node consisting of multiple fragments.
     */
    NODE
}
